# DTR S3 Configuration
# Copy this file to .env and fill in your values
# The .env file is gitignored and won't be committed

# S3 Bucket Settings
DTR_S3_BUCKET=your-bucket-name
DTR_S3_REGION=us-east-1
DTR_S3_BASE_PATH=dtr-training/
DTR_S3_CHECKPOINT_DIR=dtr-training/checkpoints/

# AWS Credentials (required unless using IAM role on EC2/ECS/EKS)
# Copy from ~/.aws/credentials or create new credentials in AWS IAM
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Optional: Custom S3 endpoint (for MinIO or S3-compatible storage)
# DTR_S3_ENDPOINT=http://localhost:9000
